/**
 * Configuration file for SFMC Custom Content Block
 * 
 * Copy this file to config.js and update with your values
 * DO NOT commit config.js to version control
 */

// Option 1: Override CONFIG in customBlock.js
// Uncomment and use this if you want a separate config file
/*
window.SFMC_CONFIG = {
    // Your Data Extension external key from SFMC
    DE_EXTERNAL_KEY: 'TranslationDataExtension',
    
    // Minimum number of fields required
    MIN_FIELDS: 1,
    
    // Maximum number of fields allowed
    MAX_FIELDS: 20,
    
    // Default fields shown on load
    DEFAULT_FIELDS: [
        { name: 'subject', value: '' },
        { name: 'preheader', value: '' },
        { name: 'header', value: '' },
        { name: 'body', value: '' },
        { name: 'footer', value: '' }
    ]
};
*/

// Option 2: Update CONFIG object directly in customBlock.js (lines 13-24)
// This is the simpler approach for most use cases
